management.health.probes.enabled=true
management.endpoint.health.show-details=always
management.endpoint.health.livenessState.enabled=true
management.endpoint.health.readinessState.enabled=true

logging.level.org.springframework=${LOG_LEVEL_SPRING:INFO} 
logging.level.ch.so.agi.sodata=${LOG_LEVEL_APP:DEBUG} 

app.configFile=${CONFIG_FILE:/config/datasearch.xml}

app.ili.ilidataDir=${ILI_ILIDATADIR:#{systemProperties['java.io.tmpdir']}}

app.stac.directory=${STAC_DIR:#{systemProperties['java.io.tmpdir']}}
app.stac.rootHref=${STAC_ROOT_HREF:https://data.geo.so.ch/stac/}
app.stac.venvParentPath=${STAC_VENV_EXE_PATH:#{systemProperties['java.io.tmpdir']}}
